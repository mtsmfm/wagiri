diff --git a/node_modules/onnxruntime-web/dist/ort.all.mjs b/node_modules/onnxruntime-web/dist/ort.all.mjs
index e07383b..b8464b7 100644
--- a/node_modules/onnxruntime-web/dist/ort.all.mjs
+++ b/node_modules/onnxruntime-web/dist/ort.all.mjs
@@ -32110,8 +32110,12 @@ fn main(@builtin(local_invocation_id) localId : vec3<u32>,
       const dimInner = aShape[aShape.length - 1];
       const dimBOuter = bShape[bShape.length - 1];
       const isVec4 = dimInner % 4 === 0 && dimBOuter % 4 === 0;
-      const elementsPerThread = dimAOuter <= 8 ? [4, 1, 1] : [4, 4, 1];
-      const workgroupSize = [8, 8, 1];
+      // wagiri patch: measured on RTX 3060, a 32x64 tile (wg[16,8], tileInner 64) is
+      // ~5% faster than the default 32x32. Can be overridden via globalThis.__MM_CFG (for experiments).
+      const mmCfg = typeof globalThis !== "undefined" && globalThis.__MM_CFG || {};
+      const elementsPerThread = dimAOuter <= 8 ? [4, 1, 1] : (isVec4 && mmCfg.ept || [4, 4, 1]);
+      const workgroupSize = dimAOuter <= 8 ? [8, 8, 1] : (isVec4 && mmCfg.wg || [16, 16, 1]);
+      const tileInner = dimAOuter <= 8 ? 32 : (isVec4 && mmCfg.ti || 64);
       const dispatch = [
         Math.ceil(dimBOuter / workgroupSize[0] / elementsPerThread[0]),
         Math.ceil(dimAOuter / workgroupSize[1] / elementsPerThread[1]),
@@ -32167,13 +32171,13 @@ fn main(@builtin(local_invocation_id) localId : vec3<u32>,
         return `
   ${shaderHelper.registerUniforms(uniforms).registerInternalVariables(batchDims).declareVariables(...inputVariables, output)}
   ${declareFunctions}
-  ${isVec4 ? makeMatMulPackedVec4Source(elementsPerThread, workgroupSize, dataType, batchDims) : makeMatMulPackedSource(elementsPerThread, workgroupSize, dataType, batchDims)}
+  ${isVec4 ? makeMatMulPackedVec4Source(elementsPerThread, workgroupSize, dataType, batchDims, false, tileInner) : makeMatMulPackedSource(elementsPerThread, workgroupSize, dataType, batchDims)}
                    `;
       };
       return {
         name: "MatMul",
         shaderCache: {
-          hint: `${elementsPerThread};${activationAttributes.activation};${isVec4};${isChannelsLast}`,
+          hint: `${elementsPerThread};${workgroupSize};${tileInner};${activationAttributes.activation};${isVec4};${isChannelsLast}`,
           inputDependencies
         },
         getRunData: () => ({
@@ -35706,7 +35710,150 @@ var init_multihead_attention = __esm({
         }
       }
     };
+    // wagiri patch: fused kernel for block-local + sink attention.
+    // Dedicated to the MHA nodes generated by conversion/fuse_attention.py
+    // (num_heads=1, q/k/v = [B*H, S, 64]). S=809 means sink8 + symmetric window ±5;
+    // otherwise full attention. Scores/accumulation in f32, online softmax, 1 dispatch/node.
+    var wagiriFusedAttention = (context, attributes) => {
+      const q = context.inputs[0];
+      const [BH, S, D] = q.dims;
+      if (D !== 64) throw new Error(`wagiriFusedAttention: unexpected head dim ${D}`);
+      const sink = S === 809 ? 8 : 0;
+      const win = S === 809 ? 5 : 0;
+      const numQBlocks = Math.ceil(S / 16);
+      const programUniforms = [
+        // Note: the C++ side sometimes does not pass the scale attribute to JS, so default to 1/sqrt(D)
+        { type: 1 /* float */, data: attributes.scale || 1 / Math.sqrt(D) },
+        { type: 12 /* uint32 */, data: S },
+        { type: 12 /* uint32 */, data: sink },
+        { type: 12 /* uint32 */, data: win },
+        ...createTensorShapeVariables(q.dims, context.inputs[1].dims, context.inputs[2].dims, [BH, S, D])
+      ];
+      const getShaderSource = (shaderHelper) => {
+        const qv = inputVariable("qq", q.dataType, 3);
+        const kv = inputVariable("kk", q.dataType, 3);
+        const vv = inputVariable("vw", q.dataType, 3);
+        const out = outputVariable("result", q.dataType, 3);
+        return `
+${shaderHelper.registerUniforms([
+          { name: "scale", type: "f32" },
+          { name: "seq_len", type: "u32" },
+          { name: "sink_n", type: "u32" },
+          { name: "win_n", type: "u32" }
+        ]).declareVariables(qv, kv, vv, out)}
+var<workgroup> Qs: array<vec4<f16>, 256>;
+var<workgroup> Ks: array<vec4<f16>, 1088>;
+var<workgroup> Sc: array<f32, 1088>;
+var<workgroup> Acc: array<f32, 1024>;
+var<workgroup> pMax: array<f32, 64>;
+var<workgroup> pSum: array<f32, 64>;
+var<workgroup> rowMax: array<f32, 16>;
+var<workgroup> rowSum: array<f32, 16>;
+var<workgroup> rowFac: array<f32, 16>;
+
+@compute @workgroup_size(256, 1, 1)
+fn main(@builtin(local_invocation_index) t: u32, @builtin(workgroup_id) wg: vec3<u32>) {
+  let S = uniforms.seq_len;
+  let qStart = wg.x * 16u;
+  let base = wg.y * S * 64u;
+  // Load Q (in vec4<f16> units, 16 rows × 16 vec4)
+  {
+    let row = t / 16u;
+    let c = t % 16u;
+    let p = qStart + row;
+    let o = base + p * 64u + c * 4u;
+    Qs[t] = select(vec4<f16>(), vec4<f16>(qq[o], qq[o + 1u], qq[o + 2u], qq[o + 3u]), p < S);
+  }
+  for (var i = t; i < 1024u; i += 256u) { Acc[i] = 0.0; }
+  if (t < 16u) { rowMax[t] = -3.0e38; rowSum[t] = 0.0; }
+  let blockFull = (uniforms.win_n == 0u) || (qStart < uniforms.sink_n);
+  let wlo = select(select(qStart - uniforms.win_n, 0u, qStart < uniforms.win_n), 0u, blockFull);
+  let whi = select(qStart + 16u + uniforms.win_n, S, blockFull);
+  let numTiles = (S + 63u) / 64u;
+  let kq = t % 64u;
+  let qg = t / 64u;
+  for (var kt = 0u; kt < numTiles; kt++) {
+    let ks = kt * 64u;
+    let ke = min(ks + 64u, S);
+    if (!(blockFull || ks < uniforms.sink_n || (ke > wlo && ks < whi))) { continue; }
+    // Load K (in vec4 units; stride 17 vec4 = 68 f16 to avoid bank conflicts)
+    for (var i = t; i < 1024u; i += 256u) {
+      let row = i / 16u;
+      let c = i % 16u;
+      let kp = ks + row;
+      let o = base + kp * 64u + c * 4u;
+      Ks[row * 17u + c] = select(vec4<f16>(), vec4<f16>(kk[o], kk[o + 1u], kk[o + 2u], kk[o + 3u]), kp < S);
+    }
+    workgroupBarrier();
+    // phase1: thread (kq, qg) computes scores for key kq × 4 queries (vec4<f16> multiply-accumulate)
+    {
+      let kp = ks + kq;
+      for (var qi = qg * 4u; qi < qg * 4u + 4u; qi++) {
+        var a4 = vec4<f16>();
+        for (var dv = 0u; dv < 16u; dv++) { a4 = a4 + Qs[qi * 16u + dv] * Ks[kq * 17u + dv]; }
+        let dot = f32(a4.x) + f32(a4.y) + f32(a4.z) + f32(a4.w);
+        let p = qStart + qi;
+        var valid = kp < S && p < S;
+        if (valid && uniforms.win_n > 0u && p >= uniforms.sink_n) {
+          valid = kp < uniforms.sink_n || (kp + uniforms.win_n >= p && kp <= p + uniforms.win_n);
+        }
+        Sc[qi * 68u + kq] = select(-3.0e38, dot * uniforms.scale, valid);
+      }
+    }
+    workgroupBarrier();
+    // phase2: per-row online softmax (t = query row)
+    if (t < 16u) {
+      var m = rowMax[t];
+      for (var j = 0u; j < 64u; j++) { m = max(m, Sc[t * 68u + j]); }
+      let fac = exp(rowMax[t] - m);
+      var s = rowSum[t] * fac;
+      for (var j = 0u; j < 64u; j++) {
+        let e = exp(Sc[t * 68u + j] - m);
+        Sc[t * 68u + j] = e;
+        s += e;
+      }
+      rowMax[t] = m; rowSum[t] = s; rowFac[t] = fac;
+    }
+    workgroupBarrier();
+    // phase3: thread (kq=dimension, qg): V read directly from global (coalesced along d)
+    {
+      let d = kq;
+      for (var qi = qg * 4u; qi < qg * 4u + 4u; qi++) {
+        var a = Acc[qi * 64u + d] * rowFac[qi];
+        for (var j = 0u; j < 64u; j++) {
+          let kp = ks + j;
+          let vj = select(f16(0), vw[base + kp * 64u + d], kp < S);
+          a += Sc[qi * 68u + j] * f32(vj);
+        }
+        Acc[qi * 64u + d] = a;
+      }
+    }
+    workgroupBarrier();
+  }
+  let d2 = t % 64u;
+  for (var qi = (t / 64u) * 4u; qi < (t / 64u) * 4u + 4u; qi++) {
+    let p = qStart + qi;
+    if (p < S) {
+      result[base + p * 64u + d2] = f16(Acc[qi * 64u + d2] / max(rowSum[qi], 1.0e-30));
+    }
+  }
+}`;;
+      };
+      context.compute({
+        name: "WagiriFusedAttention",
+        shaderCache: { hint: "wagiri-fused-attn", inputDependencies: ["type", "type", "type"] },
+        getRunData: () => ({
+          outputs: [{ dims: [BH, S, D], dataType: q.dataType }],
+          dispatchGroup: { x: numQBlocks, y: BH },
+          programUniforms
+        }),
+        getShaderSource
+      });
+    };
     multiHeadAttention = (context, attributes) => {
+      if (context.inputs.length === 3 && attributes.numHeads === 1 && context.inputs[0].dims.length === 3 && context.inputs[0].dims[2] === 64) {
+        return wagiriFusedAttention(context, attributes);
+      }
       const params = validateInputs42(context.inputs, attributes);
       const query = context.inputs[0];
       const key = getInput(context.inputs, 1);
@@ -36729,6 +36876,59 @@ var init_layer_norm = __esm({
       };
     };
     layerNorm = (context, attributes) => {
+      // wagiri patch: the LayerNormalization nodes placed by conversion/fuse_rmsnorm.py
+      // are actually L2-clip RMSNorm: y = x * gamma / max(||x||_2, 1e-4).
+      // This model has no real LayerNorm, so every 2-input LN is executed with this meaning.
+      if (context.inputs.length === 2) {
+        const x = context.inputs[0];
+        const g = context.inputs[1];
+        const dims = x.dims;
+        const d = dims[dims.length - 1];
+        let rows = 1;
+        for (let i = 0; i < dims.length - 1; i++) rows *= dims[i];
+        const programUniforms = [
+          { type: 12 /* uint32 */, data: d },
+          ...createTensorShapeVariables(dims, g.dims, dims)
+        ];
+        context.compute({
+          name: "WagiriL2Norm",
+          shaderCache: { hint: "wagiri-l2norm", inputDependencies: ["type", "type"] },
+          getRunData: () => ({
+            outputs: [{ dims, dataType: x.dataType }],
+            dispatchGroup: { x: rows },
+            programUniforms
+          }),
+          getShaderSource: (shaderHelper) => {
+            const xv = inputVariable("xx", x.dataType, dims.length);
+            const gv = inputVariable("gg", g.dataType, g.dims.length);
+            const ov = outputVariable("result", x.dataType, dims.length);
+            return `
+${shaderHelper.registerUniforms([{ name: "d", type: "u32" }]).declareVariables(xv, gv, ov)}
+var<workgroup> part: array<f32, 64>;
+@compute @workgroup_size(64, 1, 1)
+fn main(@builtin(local_invocation_index) t: u32, @builtin(workgroup_id) wg: vec3<u32>) {
+  let base = wg.x * uniforms.d;
+  var s = 0.0;
+  for (var i = t; i < uniforms.d; i += 64u) {
+    let v = f32(xx[base + i]);
+    s += v * v;
+  }
+  part[t] = s;
+  workgroupBarrier();
+  var step = 32u;
+  for (; step > 0u; step = step >> 1u) {
+    if (t < step) { part[t] = part[t] + part[t + step]; }
+    workgroupBarrier();
+  }
+  let denom = max(sqrt(part[0]), 1.0e-4);
+  for (var i = t; i < uniforms.d; i += 64u) {
+    result[base + i] = ${tensorTypeToWsglStorageType(x.dataType)}(f32(xx[base + i]) * f32(gg[i]) / denom);
+  }
+}`;
+          }
+        });
+        return;
+      }
       validateInputs46(context.inputs);
       context.compute(createLayerNormProgramInfo(context.inputs, attributes, context.outputCount));
     };
